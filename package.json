{
  "name": "dotagents",
  "private": true,
  "scripts": {
    "test": "bun test && cd scripts && bun run test"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/js-yaml": "^4.0.9"
  }
}
