{
  "name": "go-phase-harness",
  "version": "0.1.0",
  "description": "Standalone Turnlock-free phase harness for the /go pipeline.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test --timeout 60000",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "fast-check": "^3.23.2",
    "typescript": "^5.7.0"
  },
  "engines": {
    "bun": ">=1.3.12"
  }
}
