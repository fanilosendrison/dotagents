{
  "name": "@dotagents/loop-clean-protocol",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test --timeout 60000 test/portable",
    "test:repository": "bun test --timeout 60000 test/repository",
    "test:all": "bun run test && bun run test:repository && bun run mutation",
    "mutation": "bun test/mutation-runner.ts",
    "typecheck": "bun run tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.9.3"
  }
}
